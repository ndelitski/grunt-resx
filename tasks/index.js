module.exports = function (grunt) {
    grunt.registerTask('resx', 'Checking resx dictionaries for missing strings', function () {
        var done = this.async();

        this.files.forEach(function (file) {

        });

    });
};